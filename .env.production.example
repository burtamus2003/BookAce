DB_PASSWORD=
AUTH_SECRET=
RESEND_API_KEY=
# Required behind a reverse proxy — without this, Auth.js falls back to
# guessing the host from request headers, which can resolve to localhost
# and break sign-out/session redirects.
AUTH_URL=https://bookace.app
